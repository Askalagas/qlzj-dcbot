import dotenv from 'dotenv';
dotenv.config(); // Make sure env vars are loaded

export const POPE_ID = process.env.POPE_ID;
export const SCRIPTURE_CHANNEL_ID = process.env.SCRIPTURE_CHANNEL_ID;
export const GENERAL_CHANNEL_ID = process.env.GENERAL_CHANNEL_ID;